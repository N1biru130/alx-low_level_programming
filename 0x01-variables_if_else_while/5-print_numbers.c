#include <time.h>
#include <stdio.h>
#include <stdlib.h>

/*
 * Main - Entry point
 *
 * Return - Always(0) success
 */

int main (void) 
{
	int n;

	for (n = 0 ; n < 10 ; n++)
	{
		printf("%d", n);
	}
	putchar('\n');
	return (0)
}
